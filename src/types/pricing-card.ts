export type PricingCardType = {
  name: string;
  description: string;
  price: string;
  footerText?: string;
  btnText: string;
  btnLink: string;
  tagText?: string;
  important?: boolean;
  className?: string;
}