---
import CardCentered from "../ui/CardCentered.astro";
import { FaCheck } from "react-icons/fa6";

const gofitosActions = [
  { text: "Completar un tema:", points: "+50 Gofitos" },
  { text: "Completar un Pomodoro de estudio:", points: "+25 Gofitos" },
  { text: "Generar un caso práctico:", points: "+2 Gofitos" },
  { text: "Hacer un test rápido:", points: "+5 Gofitos (y más si apruebas)" },
];
---

<section class:list={["container", "py-16", "md:py-24"]}>
  <div
    class:list={[
      "grid",
      "grid-cols-1 lg:grid-cols-2",
      "gap-8 md:gap-12",
      "items-start",
      "px-4",
    ]}
  >
    <div>
      <h2 class:list={["text-3xl", "font-bold", "mb-4"]}>
        ¿Qué son los Gofitos?
      </h2>
      <p class:list={["text-neutral", "mb-6"]}>
        Los "Gofitos" son los puntos que ganas por cada acción que realizas en
        la plataforma. Son el reflejo directo de tu esfuerzo y constancia.
      </p>

      <ul class:list={["space-y-3", "mb-8"]}>
        {
          gofitosActions.map((action) => (
            <li class:list={["flex", "items-start", "gap-2"]}>
              <FaCheck className="text-success flex-shrink-0 mt-1" size={16} />
              <span>
                <strong>{action.text}</strong> {action.points}
              </span>
            </li>
          ))
        }
      </ul>

      <h3 class:list={["text-xl", "font-bold", "mb-4", "mt-6"]}>
        Sube de Nivel y Demuestra tu Progreso
      </h3>
    </div>

    <div>
      <div class:list={["grid", "grid-cols-1 md:grid-cols-3", "gap-4", "pb-0"]}>
        <CardCentered title="Guanche" iconName="book" className="py-1">
          <p class:list={["text-xs", "text-neutral"]}>0+ Gofitos</p>
        </CardCentered>

        <CardCentered title="Guayre" iconName="shield" className="py-1">
          <p class:list={["text-xs", "text-neutral"]}>100+ Gofitos</p>
        </CardCentered>

        <CardCentered title="Faycán" iconName="star" className="py-1">
          <p class:list={["text-xs", "text-neutral"]}>250+ Gofitos</p>
        </CardCentered>

        <CardCentered title="Guanarteme" iconName="trophy" className="py-1">
          <p class:list={["text-xs", "text-neutral"]}>500+ Gofitos</p>
        </CardCentered>

        <CardCentered title="Mencey" iconName="graduation" className="py-1">
          <p class:list={["text-xs", "text-neutral"]}>1000+ Gofitos</p>
        </CardCentered>
      </div>
    </div>
  </div>
</section>
