---
import Card from "../ui/Card.astro";
type IconName = "book" | "shield" | "bolt";

interface Feature {
  title: string;
  text: string;
  iconName: "book" | "shield" | "bolt";
  iconWhite?: boolean;
}

const Features: Feature[] = [
  {
    title: "1. Estudia",
    text: "Accede a un temario 100% canario, siempre actualizado y al grano",
    iconName: "book",
    iconWhite: true,
  },
  {
    title: "Practica",
    text: "Genera tests y casos prácticos con IA para dominar cada tema.",
    iconName: "shield",
    iconWhite: true,
  },
  {
    title: "Compite",
    text: "Gana puntos, sube de nivel y mídete con otras opositoras en el ranking",
    iconName: "bolt",
    iconWhite: true,
  },
];
---

<section class:list={["grid", "md:grid-cols-3", "bg-secondary", "w-full"]}>
  {
    Features.map((feature) => (
      <Card
        title={feature.title}
        text={feature.text}
        iconName={feature.iconName}
        iconWhite={feature.iconWhite}
        class:list={["bg-amber-600"]}
        className="bg-secondary flex items-center text-white text-center p-0 pt-5 max-w-xs mx-auto"
        
      />
    ))
  }
</section>
