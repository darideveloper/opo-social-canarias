---
import { FaBook, FaBrain } from 'react-icons/fa'
import { FaShieldHalved } from 'react-icons/fa6'
import { FaBolt } from 'react-icons/fa6'
import { FaBullseye } from 'react-icons/fa6'
import { FaStar } from 'react-icons/fa6'

//icons for footer
import { FaBookOpen } from "react-icons/fa";
import { LuBrainCircuit } from "react-icons/lu";
import { FaTrophy } from "react-icons/fa";

// Props
interface Props {
  title: string
  text: string
  iconName: 'book' | 'shield' | 'bolt' | 'bullseye' | 'star' | 'bookOpen' | 'trophy' | 'brain'
  iconWhite?: boolean
  textWhite?: boolean
  className?: string
}
const {
  title,
  text,
  iconName,
  iconWhite = false,
  textWhite = false,
  className = '', // FIX THIS
} = Astro.props as Props

const icons = {
  book: FaBook,
  shield: FaShieldHalved,
  bolt: FaBolt,
  bullseye: FaBullseye,
  star: FaStar,
  //icons for footer
  trophy: FaTrophy,
  bookOpen: FaBookOpen,
  brain: LuBrainCircuit
}

const Icon = icons[iconName]
---

<article class:list={['card', 'bg-base-100', 'w-full', 'shadow-sm', 'p-4', className]}>
  <div class:list={['card-body', className]}>
    <div class:list={['flex', 'items-center']}>
      <Icon
        size={36}
        className={iconWhite ? 'text-white' : 'text-secondary'}
      />
    </div>
    <h3 class:list={['card-title', 'capitalize', textWhite && 'text-white']}>
      {title}
    </h3>
    <p class:list={[textWhite && '!text-white']}>{text}</p>
  </div>
</article>
