---
export const prerender = false

// Utils components
import PageSEO from '../../components/utils/PageSEO.astro'

// Layout
import Layout from '../../layouts/Layout.astro'

return Astro.redirect('/')
---

<Layout>
  <PageSEO
    slot="seo"
    currentPage='Activación de Cuenta'
    pageDescription='Activación de Cuenta'
    extraKeywords='activate, activar cuenta'
  />
</Layout>
